{
    "_meta": {
        "description": "Telemetry event aggregation and pixel firing. Telemetry types are stateless routers that map events to pixel parameters. Pixels own the firing schedule, jitter, and parameter state."
    },
    "state": "disabled",
    "exceptions": [],
    "settings": {
        "telemetryTypes": {},
        "pixels": {}
    }
}
