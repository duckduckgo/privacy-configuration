{
    "type": "object",
    "properties": {
        "state": {"enum": ["enabled", "disabled", "beta"]},
        "exceptions": {
            "type": "array",
            "items": {"type": "object"}
        }
    },
    "required": ["state", "exceptions"]
}