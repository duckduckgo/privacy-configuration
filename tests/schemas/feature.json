{
    "type": "object",
    "properties": {
        "state": {"enum": ["enabled", "disabled", "beta"]},
        "exceptions": {
            "type": "array",
            "items": {"type": "object"}
        },
        "settings": {
            "type": "object"
        }
    },
    "additionalProperties": false,
    "required": ["state", "exceptions"]
}