{
    "type": "object",
    "properties": {
        "state": {"enum": ["enabled", "disabled", "beta"]},
        "settings": {
            "type": "object"
        },
        "minSupportedVersion": {"type": ["integer", "string"]},
        "readme": {"type": "string"}
    },
    "additionalProperties": false,
    "required": ["state"]
}