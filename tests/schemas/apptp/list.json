{
    "type": "object",
    "properties": {
        "state": {"enum": ["enabled", "disabled", "beta"]},
        "rules": {
            "type": "array",
            "items": {"type": "object"}
        }
    },
    "additionalProperties": false,
    "required": ["rules"]
}